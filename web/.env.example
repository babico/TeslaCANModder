# Web app environment variables
# Copy to .env and adjust as needed

# Production server port (default: 4173)
PORT=4173

# Allowed hosts for Vite dev server (comma-separated)
# VITE_ALLOWED_HOSTS=localhost,my-host.local
