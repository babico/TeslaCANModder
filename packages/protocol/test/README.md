# Protocol Test Layout

Tests are grouped by functionality to keep failures scoped and discoverable.

- commands/: wire command builders and command families
- format/: display/format helpers
- parser/: parser and decoder behavior
- reducer/: board-state reducer behavior
- integration/: cross-layer and end-to-end smoke checks
